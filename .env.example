# Copy to .env.local and fill. See TRACKING_SPEC.md.
NEXT_PUBLIC_WA_NUMBER=6281138867000   # WhatsApp lead number, intl format (no + / 0 / spaces)
NEXT_PUBLIC_FB_PIXEL_ID=              # Meta Events Manager → Dataset (Pixel) ID
NEXT_PUBLIC_GA4_ID=                   # GA4 → Data stream → Measurement ID (G-XXXXXXXXXX)
NEXT_PUBLIC_GSC_VERIFICATION=        # Google Search Console → HTML tag verification token
